const PropertyCard = ({
  info,
}: {
  info: {
    title: string;
    ratings: number;
    address: string;
    images: Array<string>;
  };
}) => {
  return <div className="w-[400px] h-[400px] bg-black"></div>;
};

export default PropertyCard;
